# DATABASE

```yaml
<%-schemaYaml%>
```

# QUERIES

<% for (var i = 0; i < Math.min(5, queries.length); i++) { %>
> <%-queries[i].query%>
<%-queries[i].validatedSQL.replace(/(\r\n|\n|\r)/gm, " ").replace(/\s\s+/g, " ") %>
---
<% } %>
> <%-query%>
