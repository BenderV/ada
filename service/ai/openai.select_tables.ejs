# DATABASE
- schemaName: public
  name: company
  columns: [id, name, employees count, ]
- schemaName: public
  name: company_activity_update
  columns: [company_id, activity_update, created_at, updated_at, ]
- schemaName: public
  name: products
  columns: [product_id, name, price, ]

# QUERY
> Show to 5 highest priced products
[{
	"schemaName": "public",
	"name": "products"
}]

---

# DATABASE
- schemaName: public
  name: company
  columns: [id, name, employees count, ]
- schemaName: public
  name: company_activity_update
  columns: [company_id, activity_update, created_at, updated_at, ]
- schemaName: public
  name: products
  columns: [companies, product_id, name, price, ]

# QUERY
> Show the company with the most number of products
[{
	"schemaName": "public",
	"name": "company"
},{
	"schemaName": "public",
	"name": "products"
}]

---

# DATABASE
<% for( let index = 0; index < tables.length; index++ ) { %>
- schemaName: <%-tables[index].schemaName%>
  name: <%-tables[index].name%>
  columns: [<% for( let ind = 0; ind < tables[index].columns.length; ind++ ) { %><%-tables[index].columns[ind].name%>,%> <% } %>]
<% } %>

# QUERY
> <%-query%>
