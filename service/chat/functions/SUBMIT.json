{
    "name": "SUBMIT",
    "description": "Give the official response from the previous demand / query",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "'SQL' only for now"
        },
        "query": {
          "type": "string",
          "description": "The SQL query string to be executed. Don't forget to escape this if you use double quote."
        }
      },
      "required": ["query"]
    }
}
